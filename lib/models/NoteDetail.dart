 class NoteDetail {
  String id;
  String title;
  String content;
  DateTime createDateTime;
  DateTime lastEdited;

  NoteDetail({
    this.id,
    this.title,
    this.content,
    this.createDateTime,
    this.lastEdited
  });
}